{
  // "files" — список файлов, которые будут включены в проект TypeScript. 
  // Здесь он пустой, потому что все файлы проекта обрабатываются через "references".
  "files": [],
  "references": [
    // Указывает на другой файл конфигурации TypeScript, tsconfig.node.json.
    // Этот файл, как правило, используется для настройки конфигурации TypeScript для Node.js-среды.
    { "path": "./tsconfig.node.json" },

    // Указывает на tsconfig.web.json, который обычно содержит настройки для веб-среды (браузера).
    { "path": "./tsconfig.web.json" }
  ]
}